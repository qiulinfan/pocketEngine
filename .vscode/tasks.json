{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Refresh compile_commands (unix-makefiles-debug)",
            "type": "shell",
            "command": "${workspaceFolder}/scripts/refresh_compile_commands.sh",
            "args": [
                "unix-makefiles-debug"
            ],
            "problemMatcher": []
        },
        {
            "label": "Refresh compile_commands (ninja-debug)",
            "type": "shell",
            "command": "${workspaceFolder}/scripts/refresh_compile_commands.sh",
            "args": [
                "ninja-debug"
            ],
            "problemMatcher": []
        }
    ]
}
